context.modules = [
  { name = libpipewire-module-loopback
    args = {
      node.description = "Matisse (L/R swapped)"
      capture.props = {
        node.name = "lr-swapped_sink"
        media.class = "Audio/Sink"
        audio.position = ["FL","FR"]
      }
      playback.props = {
        node.name = "playback.lr-swapped"
        audio.position = ["FR","FL"]
        target.object = "alsa_output.pci-0000_2f_00.4.analog-stereo"
        audio.channels = 2
        stream.dont-remix = true
        node.dont-reconnect = true
        node.passive = true
      }
    }
  }
]
